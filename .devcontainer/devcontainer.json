{
	"name": "Phoenix m32",
	"image": "mcr.microsoft.com/devcontainers/base:jammy",
	"mounts":[
		"source=${localEnv:HOME}/work/phoenix,target=/work,type=bind"
	],

	"postCreateCommand": "sudo dpkg --add-architecture i386 && sudo apt-get update && sudo apt-get install -y gcc-multilib make libc6-dev:i386",
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-vscode.cpptools",
				"ms-vscode.cpptools-extension-pack"
			]
		}
	}
}
