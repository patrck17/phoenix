{
	"name": "Python 3",
	"image": "mcr.microsoft.com/devcontainers/base:jammy",
	"mounts":[
		"source=${localEnv:HOME}/work/phoenix,target=/work,type=bind"
	],

	"postCreateCommand": "sudo apt-get update && sudo apt-get install -y gcc-multilib make"
}
